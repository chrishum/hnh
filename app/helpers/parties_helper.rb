module PartiesHelper
  
#  def delete_link(party, divider)
#    if current_user && current_user.admin?
#      @links = divider + link_to("delete", party, :method => :delete, :confirm => "You sure?", :title => "Delete #{party.name}")
#      @links.html_safe
#    end
#  end
end
